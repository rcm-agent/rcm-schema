# RCM Schema Environment Variables

# Database connection string
# For local development:
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/rcm_db

# For production (with connection pooling):
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Database pool settings (optional)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# Enable SQL echo for debugging (optional)
SQL_ECHO=false

# Serverless environment flag (optional)
# Set to true to disable connection pooling
SERVERLESS=false